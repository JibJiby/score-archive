import styled from '@emotion/styled'

export const MyImageWrapper = styled.div`
  position: relative;

  /* & .check-icon {
    position: absolute;
    background-color: red;
    top: 40;
    right: 20;
    user-select: none;
  } */
`
